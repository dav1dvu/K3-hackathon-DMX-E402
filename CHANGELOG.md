# CHANGELOG

## §9. Changelog

| Thời điểm                   | Đổi gì                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Vì sao (trỏ về feedback/case nào)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-30 14:08 —`de98d17` | Tạo prototype React/TypeScript/Vite đầu tiên.`react-pdf` render PDF thật với text layer, thumbnail, Previous/Next có giới hạn trang và trạng thái loading/error; màn upload kiểm tra MIME/đuôi PDF và có tài liệu mẫu. Chat lúc này vẫn dùng `setTimeout` và `buildMockAnswer`, lưu hội thoại riêng theo trang; `App.test.tsx` kiểm tra file sai loại, điều hướng, trang tại thời điểm gửi, giữ history và reset.                                                                                                                                                                                                                                    | Quyết định chọn React + TypeScript + Vite để rút ngắn vòng lặp phát triển nhưng vẫn bảo đảm type safety;`react-pdf`/PDF.js được dùng để render PDF và text layer trực tiếp trên trình duyệt. Chat được chủ động giữ ở dạng mock để xác thực độc lập luồng điều hướng, state theo trang và trải nghiệm end-to-end theo `mo_ta.md — Acceptance criteria` và `04-rubric.md — CP2`, qua đó giảm rủi ro giao diện trước khi tích hợp backend và LLM thật tại CP3.                                                                                                                                                 |
| 2026-07-30 14:44 —`d0c48e9` | Thay câu trả lời mock bằng pipeline phía trình duyệt: ưu tiên PDF text, fallback OCR qua`tesseract.js`, chia chunk có overlap, tạo lexical index, truy xuất theo slide hiện tại/toàn bài, tạo lesson overview và citation trang. Thêm `KnowledgePanel`, 5 golden cases (`current-text`, `current-ocr`, `whole-lesson`, `compare-pages`, `missing-answer`) và test ingestion/OCR/RAG.                                                                                                                                                                                                                                                                                  | Quyết định bổ sung pipeline ingestion/RAG phía trình duyệt như một lớp tri thức khả dụng sớm: ưu tiên text layer để giữ độ chính xác và dùng`tesseract.js` làm fallback cho slide dạng ảnh. Cách tiếp cận này trực tiếp xử lý khoảng trống được ghi tại `tong-hop-feedback-ai-tutor.md — §3.1–§3.4, §7 P0`: không đọc được slide ảnh, thiếu ngữ cảnh toàn bài và từng yêu cầu người dùng tự mô tả khi hỏi “Cả slide này đang nói về gì?”.                                                                                                                                                            |
| 2026-07-30 15:30 —`af91a0b` | Thêm backend Express và`LLMCore`: cấu hình provider OpenAI-compatible qua env, primary/fallback, timeout/retry, health check, structured logging không lộ prompt/key, chuẩn hóa lỗi và adapter Gemini/NVIDIA. `POST /api/tutor/chat` nhận evidence đã truy xuất, gọi LLM thật với JSON schema, kiểm tra lại `sourcePages`; frontend chuyển từ câu trả lời deterministic sang `askTutor()`. Bổ sung unit test core/provider và E2E API → grounded generation → provider stub.                                                                                                                                                                                        | Quyết định chuyển việc gọi mô hình sang backend và bao bọc các nhà cung cấp bằng`LLMCore` để bảo vệ thông tin xác thực, thống nhất structured output, hỗ trợ thay thế provider và kiểm soát timeout/retry/fallback tại một điểm. Đây là nền tảng để đáp ứng yêu cầu gọi AI thật trong `01-de-bai.md`, `04-rubric.md — CP3/R5` và grounded chat tại kiến trúc provider và observability là quyết định kỹ thuật phục vụ tích hợp, không xuất phát từ feedback trực tiếp.                                                                                                                                      |
| 2026-07-30 15:52 —`e3576e7` | Tạo framework evaluation CP3: schema/validator cho 20 case`CP3-001`…`CP3-020`, runner ghi artifact lần đầu bất biến, chuẩn đạt ≥80%, không `blocked`, zero-tolerance cho `UNSUPPORTED_ASSERTED_ANSWER` và `INVALID_CITATION`. Commit lưu kết quả thật **13/20 pass (65%), 7 fail, 3 lỗi nghiêm trọng, quality gate FAIL**; không che giấu case fail.                                                                                                                                                                                                                                                                                                              | Quyết định thiết lập evaluation framework và quality gate định lượng để biến yêu cầu CP3 thành điều kiện phát hành có thể tái lập, đồng thời lưu artifact lần đầu bất biến nhằm tránh che khuất regression. Cách đánh giá này thực thi`04-rubric.md — R4` và `eval/acceptance_standard.md`; kết quả 13/20, ba lỗi zero-tolerance và trạng thái FAIL được giữ nguyên để phản ánh đúng mức sẵn sàng của hệ thống.                                                                                                                                                                                                |
| 2026-07-30 15:52 —`e3576e7` | Mở rộng system prompt của`/api/tutor/chat` với quy tắc không đoán câu mơ hồ, từ chối hành động/công cụ ngoài khả năng và chỉ trả lời thông tin high-stakes khi evidence nêu rõ. Tuy vậy artifact cùng commit vẫn fail `CP3-004`, `CP3-007`, `CP3-008`, `CP3-012`, `CP3-015` cùng hai case khác, nên không thể coi các nhóm lỗi này đã được khắc phục hoàn toàn.                                                                                                                                                                                                                                                                            | Quyết định tăng cường system prompt như lớp kiểm soát đầu tiên cho ba nhóm rủi ro: thiếu dữ kiện, câu hỏi mơ hồ và yêu cầu vượt thẩm quyền/high-stakes, tương ứng`CP3-004..015`. Tuy nhiên `eval/eval_summary.md` vẫn ghi nhận các case `CP3-004`, `CP3-007`, `CP3-008`, `CP3-012`, `CP3-015` thất bại; vì vậy thay đổi này chỉ là biện pháp giảm rủi ro ở tầng prompt, chưa đủ cơ sở công nghệ để công nhận lỗi đã được xử lý hoàn toàn.                                                                                                                                                        |
| 2026-07-30 22:00 —`07ee898` | Thay luồng UI upload bằng tự động gọi backend để phát hiện PDF trong`data/slide`; thêm ba PDF Day 1/Day 2/Day 3, tự mở file đầu tiên và cho chọn file khi có nhiều tài liệu. Thêm năm API `/api/slides/documents...`, trả PDF với `application/pdf`, chỉ chấp nhận document ID đã discover để chặn traversal; frontend có discovering/processing/empty/error state và vẫn render từng trang thật bằng `react-pdf`. `App.test.tsx` và `server/slides/api.test.ts` kiểm tra auto-select, không có PDF, MIME, traversal và giới hạn Previous/Next. Luồng `UploadScreen` cũ vẫn còn file nhưng không còn được `App.tsx` sử dụng. | Quyết định chuyển nguồn tài liệu sang backend-managed`data/slide` và dùng document ID do server cấp để tạo một nguồn dữ liệu thống nhất, loại bỏ bước upload không cần thiết và giới hạn truy cập file trong tập tài liệu đã discover nhằm chống path traversal. Thiết kế này hiện thực hóa feedback “Project phải tự động sử dụng PDF có sẵn trong `data/slide`… Người dùng không cần upload PDF”, đồng thời đáp ứng các acceptance case về PDF thật, điều hướng có giới hạn và trạng thái không có tài liệu mà không làm backend crash.                                                     |
| 2026-07-30 22:00 —`07ee898` | Chuyển xử lý tài liệu chính sang backend`SlideDocumentService` + worker Python dùng `unstructured.partition.pdf`. Pipeline giữ `text`, `filename`, `page_number`, `element_type`, bỏ element rỗng, nhóm theo trang, tạo `lesson_context` theo thứ tự và cache JSON bằng fingerprint kích thước/mtime; map promise đang chạy ngăn partition lặp đồng thời. Test xác minh discovery rỗng, metadata, ranh giới slide, thứ tự context, cache chỉ partition một lần và ID lạ. Client OCR/RAG cũ vẫn còn cho regression test nhưng không còn nằm trên luồng UI chính.                                                                             | Quyết định dùng`unstructured.partition.pdf` tại backend làm pipeline xử lý chuẩn để bảo toàn metadata và ranh giới trang; cache theo fingerprint kích thước/mtime cùng in-flight promise được chọn để tránh chi phí partition lặp và xử lý đồng thời trùng lặp. Thiết kế bám flow bắt buộc commit mới xác minh bằng partitioner giả, còn feedback sau đó “slide Day1 và Day3 không OCR được” cho thấy chiến lược OCR thực tế vẫn cần một thay đổi riêng và chưa được commit này giải quyết.                                                                                                               |
| 2026-07-30 22:00 —`07ee898` | Thêm chat theo tài liệu tại`/api/slides/documents/{id}/chat`: request strict chỉ gồm `current_page`, `question`, `history`; backend tự tải cache, đặt CURRENT SLIDE trước LESSON CONTEXT, giới hạn history còn 6 tin/12.000 ký tự, gọi LLM structured và lọc citation trùng, ngoài tài liệu hoặc ngoài `total_pages`. UI giữ một history khi chuyển trang và citation `[Slide N]` có thể bấm để tới trang nguồn. Test bao phủ slide hiện tại, câu toàn bài, ngoài tài liệu, citation đúng/sai và body không chứa client context/system prompt.                                                                                           | Quyết định để backend sở hữu toàn bộ context assembly và chỉ nhận`current_page`, `question`, `history` từ client nhằm bảo đảm trang hiện tại là nguồn ưu tiên, tránh client tự chèn lesson context/system prompt và cho phép kiểm soát history/citation tập trung. Thiết kế đáp ứng feedback “Slide này đang nói về gì?”, yêu cầu tổng quan toàn bài, không bịa citation và giữ history khi chuyển trang, liên hệ  golden case `whole-lesson`/`missing-answer`; phản hồi Day2 sau commit cho thấy ngân sách `maxTokens: 900` vẫn là rủi ro làm JSON tổng quan bị cắt và chưa có commit khắc phục. |
| 2026-07-30 22:00 —`07ee898` | Củng cố thư viện RAG cũ bằng query understanding Việt–Anh, synonym expansion, history rewrite có kiểm soát, lookup trực tiếp khi câu hỏi nêu số trang, metadata`documentId/page range/section/topic`, section summary, truy vấn locate/count topic, kết quả `partially_answered`, citation range và lọc output kỹ thuật/lặp câu. `src/rag/bug-regression.test.ts` thêm regression cho năm cách hỏi trang 5, “Nội dung slide là gì?”, `giảng viên/instructor`, vị trí User Needs, đếm chủ đề và trang thiếu một phần. Hiện thư viện này được test nhưng không được active `App.tsx` gọi sau khi chuyển sang API slide.           | Quyết định tách query understanding, metadata lookup và output filtering thành các lớp trong thư viện RAG legacy để ưu tiên truy xuất xác định cho câu hỏi có số trang, đồng thời chuẩn hóa truy vấn Việt–Anh và giữ regression coverage cho locate/count/partial-answer. Cơ sở quyết định là các case có thật trong`src/rag/bug-regression.test.ts`và vì `App.tsx` hiện không gọi thư viện này nên đây là tài sản kiểm thử/thiết kế tương thích, không được xem là hành vi đã triển khai trên luồng sản phẩm chính.                                                                                   |
| 2026-07-30 22:00 —`07ee898` | Nâng contract legacy`/api/tutor/chat` từ `answer/sourcePages/insufficientContext` sang `status/answer/citations/missing_fields`, thêm `QUERY_CONTEXT`, citation theo range/section, lọc output và extractive fallback khi model báo thiếu sai cho intent deterministic. Eval runner được đổi để đọc citation range; test mới kiểm tra citation giả, false negative ở trang 5, current-slide summary, unsupported field và partial answer. Endpoint này vẫn tồn tại cho eval/E2E nhưng frontend chính hiện dùng `/api/slides/.../chat`.                                                                                                                           | Quyết định nâng contract legacy`/api/tutor/chat` sang schema có `status`, citation giàu metadata và `missing_fields` để backend có thể validate kết quả máy đọc được; extractive fallback được dùng cho intent xác định nhằm giảm false negative khi evidence đã đủ. Thay đổi nhắm trực tiếp regression `CP3-001` và quy tắc không citation giả trong `eval/acceptance_standard.md`, được khóa bằng `server/tutor/grounded-generation.test.ts`; do chưa có evaluation artifact mới, chưa đủ bằng chứng để kết luận quality gate tổng thể đã chuyển từ FAIL sang PASS.                                      |
